name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: SSH into EC2 and deploy
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          timeout: 60m
          command_timeout: 60m
          script: |
            set -e

            # ── Install git if not present ────────────────────────
            if ! command -v git &>/dev/null; then
              sudo apt-get update -y
              sudo apt-get install -y git
            fi

            # ── Pull latest code ──────────────────────────────────
            REPO_DIR="$HOME/portfolio"
            
            # Menyisipkan token akses otomatis agar bisa clone/pull repo private
            REPO_URL="https://${{ github.actor }}:${{ secrets.GITHUB_TOKEN }}@github.com/yassir420/Portofolio.git"
            
            if [ -d "$REPO_DIR/.git" ]; then
              git -C "$REPO_DIR" pull $REPO_URL main 
            else
              git clone $REPO_URL "$REPO_DIR"
            fi

            # ── Free up disk space before build ───────────────────
            sudo docker system prune -a -f --volumes || true

            # ── Build image on EC2 ────────────────────────────────
            sudo docker build -t portfolio:latest "$REPO_DIR"

            # ── Swap container ────────────────────────────────────
            sudo docker stop portfolio 2>/dev/null || true
            sudo docker rm   portfolio 2>/dev/null || true
            sudo docker run -d \
              --name portfolio \
              --restart unless-stopped \
              -p 80:3000 \
              portfolio:latest

            # ── Clean up old images ───────────────────────────────
            sudo docker image prune -f

            echo "[deploy] Done."